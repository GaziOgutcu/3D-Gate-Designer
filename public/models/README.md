Place the downloaded car model at this path:

```text
public/models/car.glb
```

The 3D scene loads `/models/car.glb` at runtime. If the file is unavailable, the app renders a lightweight fallback car so the scene remains usable.
